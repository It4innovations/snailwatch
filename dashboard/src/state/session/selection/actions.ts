import {Selection} from '../../../lib/measurement/selection/selection';
import {createCrudActions} from '../../../util/crud';

export const SelectionActions = createCrudActions<Selection>('selection');
