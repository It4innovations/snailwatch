Workflow
==============
The workflow of Snailwatch is very simple. Users can create projects
(those can usually be mapped 1:1 to a repository) and upload measurement data
to them using a REST API. The measured results can then be filtered,
aggregated and displayed in the web dashboard.
